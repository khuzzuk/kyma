#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../github-stats/bin/github-stats" "$@"
  ret=$?
else 
  node  "$basedir/../github-stats/bin/github-stats" "$@"
  ret=$?
fi
exit $ret
